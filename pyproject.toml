[project]
name = "clipsnstrips"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "assemblyai",
    "elevenlabs>=2.60.0",
    "google-api-python-client",
    "google-auth-oauthlib",
    "google-genai",
    "openai",
    "pydantic-settings",
    "pymupdf>=1.28.0",
    "python-docx>=1.2.0",
    "tenacity",
    "typer",
    "yt-dlp",
]

[project.scripts]
clipsnstrips = "clipsnstrips.cli:app"

[tool.uv]
package = true

[tool.setuptools.packages.find]
include = ["clipsnstrips*"]
exclude = ["downloads*", "output*", "tests*"]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py314"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
